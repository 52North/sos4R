# https://appsilon.com/continuous-integration-for-your-private-r-projects-with-circleci/
version: 2
jobs:
  build:
    working_directory: ~/main
    docker:
      - image: rocker/geospatial:3.6.2
    steps:
      - checkout
      - restore_cache:
          keys:
            # use R version in cache, cannot automate, see https://ideas.circleci.com/ideas/CCI-I-1017
            - deps1-{{ .Branch }}-{{ checksum "DESCRIPTION" }}-{{ "3.6.2" }}
            - deps1-{{ .Branch }}
            - deps1-
      - run:
          command: |
            R -e 'devtools::install_deps(dependencies = TRUE)'
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "DESCRIPTION" }}-{{ "3.6.2" }}
          paths:
            - "/usr/local/lib/R/site-library"
      - run:
          command: |
            R -e 'devtools::check()'
#      - store_artifacts:
#          path: man/
#          destination: man
